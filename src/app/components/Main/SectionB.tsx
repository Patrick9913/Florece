import React from "react";

export const SectionB: React.FC = () => {
    return (
        <section className=" w-screen h-screen flex justify-center items-center">
            Section B
        </section>
    );
}