import React from "react";
import { Navbar } from "./Navbar";

export const Header: React.FC = () => {
    return (
        <header className="fondo h-screen w-screen">
            <Navbar />
        </header>
    )
}